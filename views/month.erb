<table>
  <tr>
    <th>Item</th>
    <th>Cost</th>
    <th>Yearly Total</th>
  </tr>
  <% session[:data][params[:month]].each do |item| %>
      <tr>
        <td><%= item[0] %></td>
        <td>$<%= '%.2f' % item[1] %></td>
        <td>$<%= category_total(item[0]) %></td>
      </tr>
  <% end %>
</table>
<a href="/">
  Return to index
</a>